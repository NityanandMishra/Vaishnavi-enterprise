import AttributesManager from "@/components/admin/attributes/AttributesManager";

export const metadata = {
  title: "Attributes & Variants — Admin Portal",
};

export default function AttributesPage() {
  return <AttributesManager />;
}
